# MCP Server Environment Configuration
# Strict booleans: only "true" or "false" (case-insensitive)
# DO NOT use values like "1", "yes", "on" — script will reject them.

# -----------------------
# General Settings
# -----------------------
# Whether the launcher should attempt to provide proxy settings from .env
USE_PROXY=false

# If true, the script prints the exact command it would run and exits (no node exec)
DRY_RUN=false

WRITE_DEBUG_TERMINAL=false
WRITE_DEBUG_FILE=true

# -----------------------
# Proxy Settings (Priority Order)
# -----------------------
# The application checks variables in this order:
# 1. SOCKS5_PROXY
# 2. SOCKS4A_PROXY
# 3. SOCKS4_PROXY
# 4. HTTPS_PROXY
# 5. HTTP_PROXY
#
# Only the first non-empty value in this order will be used
#
# Example formats:
#   SOCKS5:   socks5://user:pass@proxy:1080
#   SOCKS4A:  socks4a://user:pass@proxy:1080
#   SOCKS4:   socks4://user:pass@proxy:1080
#   HTTPS:    https://user:pass@proxy:8080
#   HTTP:     http://user:pass@proxy:8080 | http://localhost:8080
#
# IPv6 examples:
#   socks5://[2001:db8::1]:1080
#   http://[2001:db8::1]:8080
#
# Note: Application handles validation - invalid URLs will be ignored
SOCKS5_PROXY=
SOCKS4A_PROXY=
SOCKS4_PROXY=
HTTPS_PROXY=
HTTP_PROXY=

# -----------------------
# TLS / Certificate
# -----------------------
# Path to corporate CA bundle or PEM certificate that Node should trust.
# Prefer absolute path. The launcher expands leading ~ to $HOME.
#
# Examples:
#   NODE_EXTRA_CA_CERTS=/etc/ssl/certs/corp-root.pem
#   NODE_EXTRA_CA_CERTS=$HOME/certs/corp-root.pem
#   NODE_EXTRA_CA_CERTS=~/certs/corp-root.pem | '~/.mitmproxy/mitmproxy-ca-cert.pem'
NODE_EXTRA_CA_CERTS=

# -----------------------
# Usage Examples
# -----------------------
# Basic SOCKS5 proxy:
#   SOCKS5_PROXY="socks5://proxy:1080"
#
# HTTP proxy with credentials:
#   HTTP_PROXY="http://user:pass@proxy:8080"
#
# Disable proxy injection but keep certificate:
#   USE_PROXY=false
#   NODE_EXTRA_CA_CERTS=/path/to/cert.pem
#
# CI validation (dry run):
#   USE_PROXY=true
#   IGNORE_SHELL=true
#   DRY_RUN=true
#
# Security Note: Avoid including credentials in .env files when possible.
# Use secret managers for sensitive credentials in production environments.
DEFAULT_SEARCH_ENGINES=bing,duckduckgo,brave
